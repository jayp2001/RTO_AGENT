import './countCard.css'

function CountCard({ color, data }) {
    return (
        <div className='card'>
            <div className={`card_header_box ${color}`}>
                J
            </div>
        </div>
    )
}

export default CountCard;